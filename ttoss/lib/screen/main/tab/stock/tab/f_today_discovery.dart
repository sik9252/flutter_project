import 'package:flutter/material.dart';

class TodayDiscovertFragment extends StatelessWidget {
  const TodayDiscovertFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red, height: 700);
  }
}
